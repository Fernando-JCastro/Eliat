# Eliat 🧬🌍

R tools for downloading GenBank sequences, extracting metadata (taxonomy, coordinates, vouchers), and exporting to CSV, Shapefile, or GeoTIFF.​

## Installation

``` r
# Install devtools if you don't have it
if (!require("remotes")) install.packages("devtools")

# Install Eliat from GitHub
remotes::install_github("Fernando-JCastro/Eliat")
```
