% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_products_counts.R
\name{get_products_counts}
\alias{get_products_counts}
\title{Count Occurrences of Gene Products in Metadata}
\usage{
get_products_counts(df, columna)
}
\arguments{
\item{df}{A data frame (e.g., output from \code{get_metadata()}).}

\item{columna}{Unquoted column name (e.g., \code{Products}).}
}
\value{
A \code{tibble} with columns \code{value} (product name) and \code{cantidad} (count).
}
\description{
Extracts and tallies gene product names (e.g., "cytochrome c oxidase subunit I")
from a comma-separated column (e.g., \code{Products} from \code{get_metadata()}).
}
\examples{
\dontrun{
meta <- get_metadata(seqs)
get_products_counts(meta, Products)
}

}
