% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_gb_seq.R
\name{search_gb_seq}
\alias{search_gb_seq}
\title{Search and Download GenBank Nucleotide Sequences}
\usage{
search_gb_seq(organismo, marcador = "", retmax = NULL, max_intentos = 5)
}
\arguments{
\item{organismo}{Character. Scientific or common name of the target organism (e.g., \code{"Homo sapiens"}).
Must match NCBI taxonomy naming.}

\item{marcador}{Character. Optional molecular marker (e.g., \code{"COI"}, \code{"16S"}, \code{"ITS"}).
Added to search term as \verb{[All Fields]}.}

\item{retmax}{Integer. Maximum number of records to retrieve \emph{per batch} (NCBI limit = 10,000).
Default: \code{NULL} → auto-adjusts (5,000 if >50k hits; else 10,000).}

\item{max_intentos}{Integer. Maximum number of retry attempts on failure (default: 5).}
}
\value{
Character vector. Each element is a raw GenBank record (text), ending with \code{"//\\n"}.
}
\description{
This function queries the NCBI Nucleotide database (GenBank) using \code{rentrez},
retrieves full GenBank flatfile records in batches, and returns them as a
character vector (one element per sequence).
}
\examples{
\dontrun{
seqs <- search_gb_seq("Drosophila melanogaster", "COI", retmax = 500)
}

}
